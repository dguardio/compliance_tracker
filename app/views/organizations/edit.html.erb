<div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 py-8">
  <div class="flex justify-between items-center">
    <h1 class="text-2xl font-bold text-gray-900">Edit Organization Profile</h1>
    <%= link_to 'Back to organization', @organization, class: "text-sm font-semibold text-indigo-600 hover:text-indigo-500" %>
  </div>

  <div class="mt-8 bg-white p-8 rounded-lg shadow">
    <%= render 'form', organization: @organization %>
  </div>
</div>
